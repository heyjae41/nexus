# CHANGELOG — EDU.AI 작업 히스토리

> 최신 순. 커밋 해시는 `git log` 기준, 상세는 각 커밋·병합 브랜치 참고.

## 2026-07-22 — E2E 를 CI 게이트에 편입

- **CI e2e 잡 신설**: PG 서비스 컨테이너 → 시드 → 인제스트 트리거 → 백엔드(8000) → vite(5173) →
  Playwright 6종 실행. 실패 시 스크린샷·로그 아티팩트 수집. (`feat/e2e-ci` + 후속 fix 2건, 4개 잡 전부 초록 확인)
- e2e 주소를 `E2E_BASE_URL`·`E2E_API` 로 주입 가능하게 개방, vite 프록시 전용 `API_PROXY_TARGET`
  변수 추가(VITE_ 접두사의 클라이언트 노출·CORS 문제 회피)
- e2e 스펙 현행화: 구 localStorage 로그인 방식 제거 → 서버 세션 복원 대기,
  쿠키 공유되는 `page.request` 사용, '자료' 태그 exact 매칭
- 브런치 수집 키워드 확장·수집 가이드 문서 보강 (`feat/brunch-keywords-and-collection-docs`)

## 2026-07-22 — 보안·운영 하드닝 & CI

- **CI 가동**: GitHub Actions(`.github/workflows/ci.yml`)가 push/PR 마다 품질 게이트
  (pytest·ruff·radon·vitest·eslint·build·jscpd) 자동 실행. 수동 실행(workflow_dispatch) 지원. (`99c9768`, `18505ae`)
- **서버 보안** (인프라, 커밋 외): ufw 방화벽 활성(공개 포트 22·80만), Docker DNAT 우회를 막는
  DOCKER-USER 규칙으로 PG(5432)·Redis(6379) 외부 차단, 백엔드 8000은 docker0 한정.
- **로그인 brute-force 방지**: nginx 가 로그인·가입 경로를 IP 당 분당 10회로 제한(429). (`221d32f`)
- **백엔드 systemd 서비스화** (인프라): `nexus-backend.service` — 재부팅 자동 기동·크래시 자동 재시작.
- **캐시 안정화**: `get_or_set` 재시도를 재귀에서 유한 루프로 전환(스택 폭주 방지). (`9f80148`)
- 수집잡 구조·운영 가이드 문서화 (`docs/COLLECTION_JOBS.md`). (`23bfbbd`)

## 2026-07-22 — 코드 품질: 중복 제거 (jscpd 클론 0 달성)

- **JSX 공용 카드·목록 컴포넌트**: cardKit(카드 셸·오버레이 뱃지·썸네일)·usePagedList 훅·
  FilterChips·Pagination·ErrorRetry/EmptyMessage·BackLink·PostCardList·useModalA11y(포커스 트랩 통합)
  신설 후 카드 3종과 뷰 8곳 재작성 — JSX 클론 17→0, 순 -144줄. (`ef4fb24`)
- **백엔드 공통화**: 모델 컬럼 팩토리, 페이지네이션 의존성·캐시드 목록 응답 헬퍼,
  커뮤니티 본인확인→쓰기→캐시 bump 흐름 통합 — 파이썬 클론 13→0. (`a455935`)
- API 클라이언트 JSON 요청 헬퍼 통합(-31줄) (`39b1a7d`), 테스트 셋업 보일러플레이트 헬퍼화
  (백엔드 6곳·프론트 22곳) (`ad58845`, `cec956a`), `.jscpd.json` 게이트 상시화(소스 포맷 전체 감시).
- **핫딜 이미지 버그 수정**: 커뮤니티 CDN 핫링크 차단(Referer→404 리다이렉트)으로 상품 이미지가
  깨지던 문제 — `referrerPolicy=no-referrer` 적용. (`31e7652`)

## 2026-07-22 — 코드리뷰 후속 조치

- 프록시 뒤 세션 쿠키 Secure 판별(X-Forwarded-Proto) (`77d5343`)
- 만료 로그인 세션 자동 정리 — 발급 시 만료 행 삭제 (`bbd16f2`)
- 밋업 참가 신청 alert → 인라인 완료 UI (브라우저 dialog 금지 불변식) (`5443a14`)

## 2026-07-16 ~ 07-22 — 기능 개발 (07-22 일괄 커밋)

> 여러 세션에 걸친 작업을 07-22 에 기능 단위로 분리 커밋. 코드리뷰(CRITICAL/HIGH 0) 통과 후 병합.

- **회원 인증·세션**: 비밀번호(PBKDF2-SHA256) 기반 로그인, HttpOnly 세션 쿠키(서버측 해시 저장),
  IDOR 가드, 온보딩 가입 플로우. (`f8c6884`)
- **비밀번호 정책 완화**: 영문·숫자 포함 8자 이상 (특수문자·대소문자 선택) — 개인정보 미취급 서비스 기준.
- **패스트캠퍼스 클래스 수집**: 공개 API 수집기(fail-closed 볼륨 가드)·카테고리 필터·홈/목록 연동. (`25ec85a`)
- **큐레이션 글 포맷 필터**(뉴스레터/컬럼/가이드) (`bbe6a82`), **밋업 카테고리 필터** (`127c61b`),
  **커뮤니티 태그 필터·글 삭제·좋아요 경합 방지** (`80531eb`), **AI핫딜 실시간 API 연동** (`70897d6`)
- **홈 히어로 개편**: 허위 문구·통계 제거 → 큐레이션 허브 정체성 카피 + 서비스 칩 6종 +
  실데이터 히어로 카드. 상단 검색창 제거(검색은 추후). CTA 버튼 제거. (`8061332`)
- DB 스키마 정비(server_default·FK 인덱스, 마이그레이션 004~011) (`22316de`),
  캐시·수집 동시성 레이스 방지 (`786e0b1`), 배포/개발 인프라 하드닝 (`0412482`)

## 2026-07-13 ~ 07-14

- PostgreSQL 데이터 이관 도구 (`7f84557`) — 실데이터 덤프 전송은 대기 중
- 프론트 개발 서버 포트 80 전환 (`02d87dc`)

## 2026-07-08 ~ 07-10

- 커뮤니티 완성: 닉네임 회원·글쓰기·댓글·좋아요 토글 (`0d1f5cc`), 마이페이지(수정·로그아웃·탈회) (`b161963`)
- meet.pl 밋업 자동 수집: event-us.kr 12시간 주기 (`d8479e0`) → luma.com AI/TECH 확장·체인 순차화 (`d4f46c6`)
- 브런치 수집 글 썸네일 (`4e5ccc4`), 글 저장 직후 즉시 인제스트(하이브리드) (`932442c`)
- 텔레그램 작가 화이트리스트(fail-closed)·작가별 대화 세션 (`7fe0e22`, `ac51e87`)

## 2026-07-07 — 프로젝트 초기 구축

- 백엔드 코어(TDD): 설정·파일명 파서·ref 링크·캐시 정책·DB 모델/리포지토리 (`5f83c4c`)
- API·인제스트·브런치 수집·스케줄러 (`5d332fe`), 시드·nexus-writer 스킬 (`9826621`)
- React 프론트엔드(디자인 명세 기반)·E2E 스모크 (`cac9435`)
- 코드리뷰 반영: 내부 API 외부 차단(nginx)·XSS 가드·좋아요 경합 방지 등 (`1648632`, `52cb9d8`)
- Docker 배포 구성·README (`98577f3`)
